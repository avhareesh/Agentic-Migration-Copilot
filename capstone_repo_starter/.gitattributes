
# Track large binary artifacts with Git LFS (install LFS before committing large files)
# Uncomment if you plan to store big files in the repo:
# models/** filter=lfs diff=lfs merge=lfs -text
# data/raw/** filter=lfs diff=lfs merge=lfs -text
